# Sui build artefacts
build/

# Sui client keystore and config — never commit private keys
sui.keystore
sui.aliases
client.yaml
.sui/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Ephemeral Sui publication files (localnet/devnet) — do not commit
Pub.*.toml